export const attraction = (attractionobj) => {
    // console.log(attractionobj)
    return `<section class = "bizarreries_preview">
    <h3>${attractionobj.name}</h3>
    <button id= "container_details--bizarre">Attraction Details:</button>
    </section>`
}